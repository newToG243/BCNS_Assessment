create .env file and define MONGO_URI as ur connection string.

FEATURES:
CRUD on task,
validation on Title and Data,
searching of task based on Title,Category(using query string),
sorting response(using query string)
